"""
Archived: compute_robot_base_placement_pose from env_utils.py

Kept as reference for Phase 5 (navigation waypoints, face-direction logic
for manipulation state machines). The function lives in env_utils.py and is
still used by other callers; this copy preserves the version that was the
sole placement method before the OccupancyGrid (Phase 2) was introduced
into trajectory_runner.py.

See: robocasa/utils/env_utils.py :: compute_robot_base_placement_pose
"""
